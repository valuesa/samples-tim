package cn.boxfish.thinking.clazz14.reflect;

/**
 * Created by LuoLiBing on 16/9/28.
 * 反射: 运行时类信息
 * RTTI的限制: 类型在编译时必须已知,这样才能使RTTI识别它,在编译时,编译器必须知道所有要通过RTTI来处理的类
 *
 * 反射的动机:
 * 1 获取一个不再程序空间中的对象引用.
 * 2 远程方法调用
 *
 * RTTI和反射的真正区别:
 * 对于RTTI来说,编译器在编译时打开和检查.class文件
 * 而对于反射机制来说,.class文件在编译时不可获取,在运行时打开和检查.class文件
 *
 */
public class ReflectTest {
}
